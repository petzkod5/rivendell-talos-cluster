apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: hermes-webui
  namespace: hermes
  annotations:
    app.megascope.io/name: Hermes WebUI
    app.megascope.io/group: Platform
    app.megascope.io/icon: bot
spec:
  parentRefs:
    - group: gateway.networking.k8s.io
      kind: Gateway
      name: traefik-gateway
      namespace: traefik
      sectionName: web
  hostnames:
    - hermes-ui.home.local
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      filters:
        - type: ExtensionRef
          extensionRef:
            group: traefik.io
            kind: Middleware
            name: authentik-forwardauth
      backendRefs:
        - group: ""
          kind: Service
          name: hermes-webui
          port: 8787
          weight: 1
